def removeConsecutiveCharacter(self, S):
        stack=[]
        stack.append(S[0])
        for i in range(1,len(S)):
            if stack[-1]!=S[i]:
                stack.append(S[i])
            
                    
                
        return("".join(stack))
